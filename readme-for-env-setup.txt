 Run the env-setup script (with superuser privilages) to pre-configure the environment for devstack
 
After running the script, a user (stack) would have been created.

> Run 'passwd stack' to change the password for stack
> login to teh shell as stack (or su stack)
> navigate to /opt/devstack
> change the portions marked with xxx to suit your environment
>run stack.sh
